---
{{- $architecture := or .architecture "amd64"}}
architecture: {{$architecture}}

actions:
  - action: pacstrap
    config: pacman.conf
    mirror: mirrorlist
    packages:
      - base
      - procps
