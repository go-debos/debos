architecture: amd64

actions:
  # This template action is expected to error out because the mandatory
  # 'source' property is missing.
  - action: template
    description: Template with no source
    destination: /etc/foo
