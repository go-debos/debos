architecture: amd64

actions:
  - action: image-partition
    description: Partition the image
    imagename: test.img
    imagesize: 1GiB
    partitiontype: msdos
    diskid: deadbeef
    mountpoints:
      - mountpoint: /
        partiton: system # Misspelled on purpose
    partitions:
      - name: system
        fs: ext4
        start: 0%
        end: 1GiB
